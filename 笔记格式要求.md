---
title: “笔记格式要求”
date: 2026-05-03
updated: 2026-05-05
author: RUINA3S
tags: [markdown, YAML, note, AI]
category: [备忘录, 规则]
description: "md笔记的书写规则要求记录，以及如此书写原因说明"
status: 已完成
draft: false
---

# 笔记格式要求

> 约束自身，宽容己外
>
> ——RUINA3S

## 0.为什么有这篇笔记

我想使用md文件来作为我的笔记库，在本地、在云端，建设网站，让AI管理、思考。那么兼容性强且规范的书写规则比不可少。以下开始为笔记书写规则的每一部分说明。

## 1.YAML Frontmatter（前置元数据）

使用元数据来添加文件的关键信息，可以使得AI、静态网站生成器（如 Hugo、Jekyll、Hexo）、笔记软件（如 Obsidian）等工具读取。有了这些需求，所以采用**键**使用英文，而**值**可以使用中文的形式来加强兼容性。以下将用本文的**YAML Frontmatter源码**来进行说明：

```markdown
---
title: "笔记格式要求"
date: 2026-05-03
up
author: RUINA3S
tags: [markdown, YAML, note, AI]
category: [备忘录, 规则]
description: "md笔记的书写规则要求记录，以及如此书写原因说明"
status: 记录中
draft: true
---
```

### 1.1.基础语法

- 使用 `key: value` 的键值对格式
- 键名一般用小写英文，单词间用下划线 `_` 连接
- 值可以是：字符串、数字、布尔值、列表、日期等
- 字符串可加引号也可以不加
- 有多个值时，使用数组来表示：`key: [value1, value2, ...]`

### 1.2.基础部分

```markdown
title: “笔记格式要求”
date: 2026-05-03
updated: 2026-05-05
author: RUINA3S
```

title(**标题**)：由于标题可能有其他标点符号，所以该字符串使用引号
date(**日期**)：时间不重要，有日期就好，文档生成日期
updated(**修改日期**)：文档修改日期，示例中为添加此处的日期，后续文档开头可能与示例不相同
author(**作者**)：对于我来说作者名可能会变但绝不会有标点符号

### 1.3.说明部分

```markdown
tags: [markdown, YAML, note, AI]
category: [备忘录, 规则]
description: "md笔记的书写规则要求记录，以及如此书写原因说明"
```

tags(**关键词**)：如同论文的关键词，用于帮助我们了解主要内容
category(**分类**)：比起单一分类，多个分类更能细致表示内容类型
description(**描述**)：比起描述更像我对于笔记内容的感受，此处随心就好

### 1.4.状态部分

```markdown
status: 记录中
draft: true
```

status(**状态**)：全靠个人规定，但需一致。个人目前分为数个阶段：**记录中**（书写阶段），**已完成**（结束书写，但可能会添加内容），**已定稿**（结束书写且不加内容了，哪怕内容以后失效）
draft(**草稿、未发布**):纯**布尔值**，即使用`true`（是草稿）和`false`（不是草稿），个人用来区别为**不发布**（选用`true`）还是**发布**（选用`false`）

## 2.文内基本部分

这部分开始主要使用markdown基础语法，其中非常常见的就不做具体说明

### 2.1.标题

使用markdown提供的 `#` 到 `######` 表示层级，注意不要跳级。

### 2.2.链接、图片

链接：[哔哩哔哩](https://www.bilibili.com)	代码：`[哔哩哔哩](https://www.bilibili.com)`

图片（仅展示格式）：`![图片文字](图片链接)`

### 2.3.引用、标注

用 `**粗体**` 表示核心概念，`*斜体*` 表示轻微强调或术语。

用 `>` 引用外部资料或自己的批注，方便 AI 区分原始内容与思考。

### 2.4.列表

无序列表用 `-` 或 `*`
有序列表用 `1. 2. 3.`
任务列表用 `- [ ]` 和 `- [x]`

- [x] 已完成
- [ ] 未完成

### 2.5.代码

行内代码用 `` ``，代码块用 ````` 并标注语言`（如 `python`、`json`），避免 AI 误解代码为普通文本。标注是必须项。

### 2.6.表格

```markdown
| 表头 | 表头 | 表头 |
| :--- | :--- | :--- |
| 1    | 2    | 3    |
```


## 3.扩展内容

这部分开始就是可能很久都用不上的内容了。要求不严格。

### 3.1.数学公式

当然使用`LaTeX`公式，毋庸置疑。

### 3.2.图示

流程图、时序图等，我选择用`mermaid`，不过我现在不太熟悉语法，还需学习。

### 3.3.网页内容

一般情况不会有使用情景。

