---
title: 本地大模型配置
date: 2026-08-2
author: 本地大模型配置
tags: [ollama, qwen2.5-coder-7b, vscode, kilo-code]
categories: [技术, 大模型]
description: 本地大模型配置，包括 Ollama 部署、VSCode 插件配置等（本文主要由kimi编写）
draft: true
---


## 一、Ollama 本地部署 Qwen2.5-Coder-7B
### 1. 安装 Ollama

官网下载 .exe 安装包，全程下一步

Ollama 官方安装程序默认装到 C:\Users\<用户名>\AppData\Local\Programs\Ollama，但支持通过命令行参数指定其他位置：
操作步骤：
1. 从官网下载 OllamaSetup.exe
2. 在目标盘（如 D 盘）新建文件夹，例如 D:\Ollama
3. 把安装包放进该文件夹
4. 在该文件夹路径栏输入 cmd 回车，打开命令窗口
5. 执行：
```bash
OllamaSetup.exe /DIR="D:\Ollama"
```
后续更新 Ollama 时，不要直接双击新的安装包，否则可能重新装回 C 盘。正确做法是把新安装包放到自定义目录，再次用 /DIR 参数安装。

安装完成后，确保 Ollama 服务在运行：
```bash
ollama serve
```
### 2. 拉取 Qwen2.5-Coder-7B 模型
在另一个终端窗口执行（ollama serve 需保持运行）：
```bash
ollama pull qwen2.5-coder:7b
```
下载完成后验证：
```bash
ollama list
```
应能看到 qwen2.5-coder:7b 在列表中。

自定义模型目录：

比如想放到 D 盘的指定文件夹下：`D:\Ollama\Models`

**设置环境变量**：右键"此电脑" → 属性 → 高级系统设置 → 环境变量 → 用户变量中点击新建：
| 变量名             | 变量值                |
| --------------- | ------------------ |
| `OLLAMA_MODELS` | `D:\Ollama\Models` |
重启 Ollama 后再启动服务，即可生效。

### 3. 硬件与性能预期
+ 显存需求：Qwen2.5-Coder-7B（Q4_K_M 量化）约需 5.5GB 显存
+ 最低 GPU：RTX 3060 6GB、RTX 4060 即可流畅运行
+ 运行速度：RTX 3060 上约 55 tokens/秒
+ 纯 CPU 模式：无独显也可运行，速度约 8–12 tok/s，完全可用
### 4. 验证 Ollama API 是否正常
```bash
curl http://localhost:11434/api/tags
```
如果返回 JSON 格式的模型列表，说明服务正常。
## 二、VSCode Kilo Code 插件配置
### 1. 安装 Kilo Code 扩展
1. 打开 VSCode
2. 按 Ctrl+Shift+X（macOS Cmd+Shift+X）打开扩展市场
3. 搜索 "Kilo Code"
4.点击 Install 安装
### 2. 配置 Ollama 提供商
Kilo Code 官方明确支持 Ollama 本地模型，配置步骤如下：
1. 点击 Kilo Code 面板左上角的 ⚙️ Settings（齿轮图标）
2. 切换到 Providers 标签页
3. 点击添加提供商，选择 Ollama
4. 填写以下信息：

| 配置项          | 值                                                |
| ------------ | ------------------------------------------------ |
| **Base URL** | `http://localhost:11434`（默认，如 Ollama 在本地运行则无需修改） |
| **API Key**  | 无需填写（本地运行不需要）                                    |
| **Model**    | `ollama/qwen2.5-coder:7b`                        |

Kilo Code 中本地 Ollama 模型的命名格式为 `ollama/<model_name>`，例如 ollama/qwen2.5-coder:7b。
### 3. 关键：配置 Context Window（上下文窗口）
这一步非常重要，Ollama 默认会截断 prompt 到很短的长度，必须手动调大：
+ 在 Kilo Code 的 API Provider Settings 中找到 "Context Window Size (num_ctx)"
+ 设置为至少 32768（32K）
+ 如果硬件允许，可设为 65536（64K）获得更好效果

| 你的显存                            | 模型加载后剩余 | 建议最大上下文        | 效果                     |
| ------------------------------- | ------- | -------------- | ---------------------- |
| **4GB**                         | 不够装模型   | —              | 建议换更小模型（1.5B）或纯 CPU 模式 |
| **6GB**（如 RTX 3060）             | ~0.5GB  | **4K ~ 8K**    | 勉强能用，再大就会掉速到内存         |
| **8GB**（如 RTX 3060 Ti / 4060）   | ~2.5GB  | **16K ~ 32K**  | 32K 以内基本流畅             |
| **12GB**（如 RTX 3060 12G / 4070） | ~6.5GB  | **32K ~ 64K**  | 64K 以内很稳               |
| **16GB+**                       | 充裕      | **64K ~ 128K** | 可以拉满                   |


注意：增大上下文窗口会增加内存占用，可能降低性能，需根据你的硬件调整。

### 4. 配置 API 请求超时
本地模型推理较慢，建议调高超时时间：
+ VS Code 扩展面板 → Kilo Code 齿轮菜单 → Settings → API Request Timeout
+ 建议设为 10 分钟以上（默认 10 分钟，本地模型容易触发超时）
### 5. 如果模型不在列表中：手动注册
如果 qwen2.5-coder:7b 没有出现在 Kilo Code 的模型选择器中，可以通过 kilo.json 配置文件手动注册为自定义模型：
```json
{
  "model": "ollama/qwen2.5-coder:7b",
  "provider": {
    "ollama": {
      "models": {
        "qwen2.5-coder:7b": {
          "name": "Qwen2.5 Coder 7B",
          "tool_call": true,
          "limit": {
            "context": 32768,
            "output": 8192
          }
        }
      }
    }
  }
}
```
配置文件位置：
+ macOS/Linux: ~/.config/kilo/kilo.json
+ Windows: %APPDATA%\kilo\kilo.json
### 6. 切换模型并开始使用
1. 在 Kilo Code 面板顶部的模型下拉框中，选择 ollama/qwen2.5-coder:7b
2. 建议先切换到 Ask Mode 测试连接
3. 输入一个简单编程问题验证是否正常工作
## 三、常见问题排查
| 问题           | 解决方案                                                               |
| ------------ | ------------------------------------------------------------------ |
| **连接被拒绝**    | 确认 `ollama serve` 正在运行，且端口为 11434                                  |
| **模型响应极慢**   | 降低 `num_ctx`（如从 64K 降到 32K 或 16K）；或关闭 MCP 工具加速                     |
| **工具调用失败**   | 本地 7B 模型能力有限，比云端大模型更容易出现工具调用错误，可尝试修改 prompt 或使用 Enhance Prompt 按钮  |
| **中文乱码**     | Qwen 系列原生支持中文，如出现问题检查模型是否完整下载，可重新执行 `ollama pull qwen2.5-coder:7b` |
| **显存不足 OOM** | 使用更小的上下文窗口（如 4096 或 8192），或换用更小模型如 `qwen2.5-coder:1.5b`            |





