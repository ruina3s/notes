---
title: 本地大模型配置
date: 2026-08-2
author: 本地大模型配置
tags: [ollama, qwen2.5-coder-7b, vscode, kilo-code]
categories: [技术, 大模型]
description: 本地大模型配置，包括 Ollama 部署、VSCode 插件配置等（本文主要由kimi编写）
draft: true
---


## 一、Ollama 本地部署 Qwen2.5-Coder-7B
### 1. 安装 Ollama

官网下载 .exe 安装包，全程下一步

Ollama 官方安装程序默认装到 C:\Users\<用户名>\AppData\Local\Programs\Ollama，但支持通过命令行参数指定其他位置：
操作步骤：
1. 从官网下载 OllamaSetup.exe
2. 在目标盘（如 D 盘）新建文件夹，例如 D:\Ollama
3. 把安装包放进该文件夹
4. 在该文件夹路径栏输入 cmd 回车，打开命令窗口
5. 执行：
```bash
OllamaSetup.exe /DIR="D:\Ollama"
```
后续更新 Ollama 时，不要直接双击新的安装包，否则可能重新装回 C 盘。正确做法是把新安装包放到自定义目录，再次用 /DIR 参数安装。

安装完成后，确保 Ollama 服务在运行：
```bash
ollama serve
```
### 2. 拉取 Qwen2.5-Coder-7B 模型
在另一个终端窗口执行（ollama serve 需保持运行）：
```bash
ollama pull qwen2.5-coder:7b
```
下载完成后验证：
```bash
ollama list
```
应能看到 qwen2.5-coder:7b 在列表中。

自定义模型目录：

比如想放到 D 盘的指定文件夹下：`D:\Ollama\Models`

**设置环境变量**：右键"此电脑" → 属性 → 高级系统设置 → 环境变量 → 用户变量中点击新建：
| 变量名             | 变量值                |
| --------------- | ------------------ |
| `OLLAMA_MODELS` | `D:\Ollama\Models` |
重启 Ollama 后再启动服务，即可生效。

### 3. 硬件与性能预期
+ 显存需求：Qwen2.5-Coder-7B（Q4_K_M 量化）约需 5.5GB 显存
+ 最低 GPU：RTX 3060 6GB、RTX 4060 即可流畅运行
+ 运行速度：RTX 3060 上约 55 tokens/秒
+ 纯 CPU 模式：无独显也可运行，速度约 8–12 tok/s，完全可用
### 4. 验证 Ollama API 是否正常
```bash
curl http://localhost:11434/api/tags
```
如果返回 JSON 格式的模型列表，说明服务正常。

### 5.注册ollama本地服务
需要用 NSSM（最稳定）把 ollama serve 封装成服务。

1. 下载 NSSM
```powershell
# 用 PowerShell 下载（管理员权限）
Invoke-WebRequest -Uri "https://nssm.cc/release/nssm-2.24.zip" -OutFile "$env:TEMP\nssm.zip"
Expand-Archive -Path "$env:TEMP\nssm.zip" -DestinationPath "$env:TEMP\nssm" -Force
```
或者手动下载：https://nssm.cc/download

2. 安装为系统服务
找到你的 ollama.exe 路径（通常是 C:\Users\你的用户名\AppData\Local\Programs\Ollama\ollama.exe），然后执行：
```powershell
# 进入 nssm 目录（根据你的系统选 win64 或 win32）
cd "$env:TEMP\nssm\nssm-2.24\win64"

# 安装服务
.\nssm.exe install Ollama
```
会弹出一个窗口，填写：
Path: C:\Users\你的用户名\AppData\Local\Programs\Ollama\ollama.exe
Startup directory: C:\Users\你的用户名\AppData\Local\Programs\Ollama
Arguments: serve
然后点击 Install service。

3. 启动并设为开机自启
```powershell
# 启动服务
Start-Service Ollama

# 设为自动启动
Set-Service Ollama -StartupType Automatic
```
验证服务是否正常
```powershell
# 检查服务状态
Get-Service Ollama

# 测试 API 是否通
curl http://localhost:11434
如果返回 Ollama is running，就成功了。以后开机自动后台运行，Continue.dev 直接可用。
```


## ~~二、VSCode Kilo Code 插件配置~~（已经放弃该方案）
### 1. 安装 Kilo Code 扩展
1. 打开 VSCode
2. 按 Ctrl+Shift+X（macOS Cmd+Shift+X）打开扩展市场
3. 搜索 "Kilo Code"
4.点击 Install 安装
### 2. 配置 Ollama 提供商
Kilo Code 官方明确支持 Ollama 本地模型，配置步骤如下：
1. 点击 Kilo Code 面板左上角的 ⚙️ Settings（齿轮图标）
2. 切换到 Providers 标签页
3. 点击添加提供商，选择 Ollama
4. 填写以下信息：

| 配置项          | 值                                                |
| ------------ | ------------------------------------------------ |
| **Base URL** | `http://localhost:11434`（默认，如 Ollama 在本地运行则无需修改） |
| **API Key**  | 无需填写（本地运行不需要）                                    |
| **Model**    | `ollama/qwen2.5-coder:7b`                        |

Kilo Code 中本地 Ollama 模型的命名格式为 `ollama/<model_name>`，例如 ollama/qwen2.5-coder:7b。
### 3. 关键：配置 Context Window（上下文窗口）
这一步非常重要，Ollama 默认会截断 prompt 到很短的长度，必须手动调大：
+ 在 Kilo Code 的 API Provider Settings 中找到 "Context Window Size (num_ctx)"
+ 设置为至少 32768（32K）
+ 如果硬件允许，可设为 65536（64K）获得更好效果

| 你的显存                            | 模型加载后剩余 | 建议最大上下文        | 效果                     |
| ------------------------------- | ------- | -------------- | ---------------------- |
| **4GB**                         | 不够装模型   | —              | 建议换更小模型（1.5B）或纯 CPU 模式 |
| **6GB**（如 RTX 3060）             | ~0.5GB  | **4K ~ 8K**    | 勉强能用，再大就会掉速到内存         |
| **8GB**（如 RTX 3060 Ti / 4060）   | ~2.5GB  | **16K ~ 32K**  | 32K 以内基本流畅             |
| **12GB**（如 RTX 3060 12G / 4070） | ~6.5GB  | **32K ~ 64K**  | 64K 以内很稳               |
| **16GB+**                       | 充裕      | **64K ~ 128K** | 可以拉满                   |


注意：增大上下文窗口会增加内存占用，可能降低性能，需根据你的硬件调整。

### 4. 配置 API 请求超时
本地模型推理较慢，建议调高超时时间：
+ VS Code 扩展面板 → Kilo Code 齿轮菜单 → Settings → API Request Timeout
+ 建议设为 10 分钟以上（默认 10 分钟，本地模型容易触发超时）
### 5. 如果模型不在列表中：手动注册
如果 qwen2.5-coder:7b 没有出现在 Kilo Code 的模型选择器中，可以通过 kilo.json 配置文件手动注册为自定义模型：
```json
{
  "model": "ollama/qwen2.5-coder:7b",
  "provider": {
    "ollama": {
      "models": {
        "qwen2.5-coder:7b": {
          "name": "Qwen2.5 Coder 7B",
          "tool_call": true,
          "limit": {
            "context": 32768,
            "output": 8192
          }
        }
      }
    }
  }
}
```
配置文件位置：
+ macOS/Linux: ~/.config/kilo/kilo.json
+ Windows: %APPDATA%\kilo\kilo.json
### 6. 切换模型并开始使用
1. 在 Kilo Code 面板顶部的模型下拉框中，选择 ollama/qwen2.5-coder:7b
2. 建议先切换到 Ask Mode 测试连接
3. 输入一个简单编程问题验证是否正常工作

## 三、Continue.dev 配置
打开 Continue 的配置（~/.continue/config.yaml 或点击齿轮），编写`.yaml`文件。`.yaml`语法如下：
| 规则             | 示例                   | 说明                 |
| -------------- | -------------------- | ------------------ |
| **缩进用空格**      | `  - name: xxx`      | 不能用 Tab，通常 2 个空格一级 |
| **列表用 `-`**    | `- provider: ollama` | 每个 `-` 代表数组里的一个元素  |
| **键值对用 `:`**   | `name: Qwen3-8B`     | 冒号后面**必须有空格**      |
| **多行文本用 `\|`** | `prompt: \|`         | 保留换行和格式            |
| **注释用 `#`**    | `# 这是注释`             | 整行或行尾都可以           |



找ai写了一个模板:
```yaml
# ============================================================
# Continue.dev config.yaml —— 从零开始学配置（中文注释版）
# ============================================================
# 这份文件是 Continue 的"大脑"，它决定了：
#   1. 用哪个 AI 模型（本地/云端）
#   2. 模型负责什么任务（聊天/补全/改代码）
#   3. AI 的行为规则（文风、编码规范）
#   4. 你能用哪些快捷命令（/写文章 /做网页）
#   5. AI 能读取哪些上下文（代码库、终端报错）
#
# 文件位置：
#   Windows: %USERPROFILE%\.continue\config.yaml
#   Mac/Linux: ~/.continue/config.yaml
#   项目级: 你的项目/.continue/config.yaml（覆盖全局配置）
#
# 修改后保存，Continue 会自动刷新，无需重启 VS Code。
# ============================================================


# -----------------------------------------------------------
# 第一部分：基础信息（必须）
# -----------------------------------------------------------
name: 我的办公AI配置          # 配置名称，随便起，只在设置界面显示
version: 1.0.0                # 配置版本，你自己管理
schema: v1                    # 固定写 v1，表示用新版 YAML 格式


# -----------------------------------------------------------
# 第二部分：模型配置（核心）
# -----------------------------------------------------------
# models 是一个列表（数组），每个 `-` 开头代表一个模型。
# Continue 会根据 "roles" 字段，自动把任务分配给合适的模型。
#
# 【roles 可选值】
#   chat       → 侧边栏聊天、问答
#   edit       → 选中代码后让 AI 修改（Inline Edit）
#   apply      → AI 生成代码块后，自动应用到文件
#   autocomplete → 按 Tab 键时的代码补全
#   embed      → 代码库语义搜索（需要专门的嵌入模型）
#   rerank     → 优化搜索结果排序
#   summarize  → 总结长文本（目前很少用）
#
# 【provider 可选值】
#   ollama     → 本地模型（免费、隐私、你的场景）
#   openai     → OpenAI API
#   anthropic  → Claude API
#   gemini     → Google Gemini
#   其他还有 azure、bedrock、xai 等
#
# 一个模型可以同时拥有多个 roles，比如聊天+编辑用同一个模型。
# -----------------------------------------------------------

models:
  # ========== 模型 1：主力模型（聊天 + 编辑 + 应用）==========
  - name: Qwen3-8B              # 你给这个模型起的名字，随意
    provider: ollama            # 提供方：ollama 表示本地运行
    model: qwen3:8b             # Ollama 里的模型名，必须和 ollama list 显示的一致
    apiBase: http://localhost:11434   # Ollama 默认地址，一般不用改
    roles:                      # 这个模型负责哪些任务
      - chat
      - edit
      - apply
    # --- 生成参数（可选，控制 AI 输出风格）---
    defaultCompletionOptions:
      temperature: 0.3          # 随机性：0.0=最保守/准确，1.0=最有创意
      maxTokens: 4096           # 单次回复最多多少字（token）
      contextLength: 8192       # 上下文窗口大小，8B模型建议 4K-8K

  # ========== 模型 2：代码补全专用（可选）==========
  # 如果你之后想加一个小模型专门做 Tab 补全，可以这样写：
  # - name: Qwen-Coder-1.5B
  #   provider: ollama
  #   model: qwen2.5-coder:1.5b
  #   roles:
  #     - autocomplete
  #   autocompleteOptions:      # 补全专属配置
  #     debounceDelay: 300       # 输入停止后多少毫秒触发补全（防卡顿）
  #     maxPromptTokens: 512     # 每次补全最多传多少 token 给模型


# -----------------------------------------------------------
# 第三部分：AI 行为规则（全局人设）
# -----------------------------------------------------------
# rules 是一个字符串列表，每行就是一条"铁律"。
# 所有对话开始前，Continue 会自动把这些规则塞给 AI。
# 相当于 Cursor 的 .cursorrules 或 Claude Code 的 SKILL.md。
#
# 写法技巧：
#   - 用 `-` 开头，每条规则一行
#   - 规则越具体越好
#   - 可以写文风、编码规范、禁止事项
# -----------------------------------------------------------

rules:
  - 你是一位资深办公助手，擅长中文写作和轻量级网页开发。
  - 文风要求：正式但不过于生硬，适合企业内部沟通；段落清晰，关键结论前置。
  - 编码要求：优先使用原生 HTML + Tailwind CSS，保持代码简洁。
  - 禁止编造数据，不确定时标注[待确认]。


# -----------------------------------------------------------
# 第四部分：上下文提供者（AI 能"看到"什么）
# -----------------------------------------------------------
# context 决定你在聊天时，可以用 @ 引用哪些信息。
# 这些 provider 是 Continue 内置的，名字固定，不能乱写。
#
# 【常用 provider 清单】
#   codebase     → @codebase —— 整个项目的语义搜索（最常用）
#   diff         → @diff —— 当前 Git 变更（代码审查神器）
#   terminal     → @terminal —— 终端最近输出（调试报错时超好用）
#   problems     → @problems —— VS Code 错误/警告面板
#   folder       → @folder —— 某个文件夹下的所有文件
#   currentFile  → @currentFile —— 当前正在编辑的文件
#   code         → @code —— 当前选中的代码块
#   file         → @file —— 选择某个具体文件
#   docs         → @docs —— 外部文档（如 React 官方文档）
#   url          → @url —— 网页内容
#   commit       → @commit —— Git 提交历史
#   search       → @search —— 代码库文本搜索
#   tree         → @tree —— 目录树结构
#
# 注意：有些 provider 需要额外参数（如 docs），见下方示例。
# -----------------------------------------------------------

context:
  - provider: codebase
  - provider: diff
  - provider: terminal
  - provider: problems
  - provider: folder
  - provider: currentFile
  # --- 带参数的例子：索引外部文档 ---
  # - provider: docs
  #   params:
  #     sites:
  #       - name: React
  #         startUrl: https://react.dev/reference/react
  #       - name: Tailwind
  #         startUrl: https://tailwindcss.com/docs


# -----------------------------------------------------------
# 第五部分：自定义 Prompts（你的 Skill 库）
# -----------------------------------------------------------
# prompts 就是"快捷指令"，在 Continue 侧边栏输入 / 就能调用。
# 每个 prompt 是一个模板，{{input}} 会被你输入的内容替换。
#
# 【结构说明】
#   name        → /后面的命令名，如 /write
#   description → 鼠标悬停时显示的说明
#   prompt      → 发给 AI 的完整指令模板
#
# 【prompt 里的特殊占位符】
#   {{input}}   → 你在 /命令 后面输入的内容（必须）
#   {{filepath}}→ 当前文件路径（可选）
#
# 多行文本用 | 开头，表示保留换行和缩进。
# -----------------------------------------------------------

prompts:
  # ========== Prompt 1：文章写作 ==========
  - name: write
    description: 按企业文风写文章/报告/通知/邮件
    prompt: |
      请按以下规范撰写文档：

      ## 文风要求
      - 正式但不过于生硬，适合企业内部沟通
      - 段落清晰，使用小标题分层
      - 关键结论前置（金字塔原理）
      - 不使用"众所周知"等空话，不编造数据

      ## 输出格式
      - 默认输出 Markdown
      - 包含"摘要-正文-行动项"三部分
      - 不确定的信息标注 [待确认]

      ## 用户指令
      {{input}}

  # ========== Prompt 2：网页开发 ==========
  - name: webpage
    description: 创建小型网页/活动页/展示页
    prompt: |
      请创建一个轻量级网页，遵循以下规范：

      ## 技术栈
      - HTML5 + Tailwind CSS（CDN 引入）
      - 少量原生 JavaScript，不引入框架
      - 响应式设计优先

      ## 目录规范
      - 单文件优先，直接输出完整 HTML
      - 中文内容必须 lang="zh-CN"
      - 图片使用 placeholder 或 SVG 占位

      ## 编码约束
      - 语义化标签（header, main, section, footer）
      - 不使用内联 style，全部用 Tailwind 类名
      - 包含基础的移动端适配

      ## 用户需求
      {{input}}

  # ========== Prompt 3：周报生成 ==========
  - name: weekly
    description: 根据工作事项生成周报
    prompt: |
      请将以下工作事项整理成周报格式：

      ## 格式
      - 标题：【部门】XXX 第 X 周工作总结
      - 本周完成：3-5 条，每条含"事项+成果"
      - 下周计划：2-3 条，具体可执行
      - 需要协调：如有则列出

      ## 要求
      - 用数据量化成果（如"完成 3 份文档"）
      - 语言简洁，每条不超过 30 字
      - 整体控制在 300 字以内

      ## 原始事项
      {{input}}

  # ========== Prompt 4：代码解释 ==========
  - name: explain
    description: 解释选中代码的功能和逻辑
    prompt: |
      请解释以下代码，要求：
      1. 一句话说明"这段代码做什么"
      2. 逐行或逐块解释关键逻辑
      3. 指出潜在问题或优化点
      4. 假设读者是初级开发者

      {{input}}


# -----------------------------------------------------------
# 第六部分：MCP 工具（高级，可选）
# -----------------------------------------------------------
# MCP = Model Context Protocol，让 AI 能调用外部工具。
# 比如让 AI 直接查数据库、操作文件系统、搜索网页等。
# 这是 Continue 的 Agent 模式的核心能力。
#
# 初学者可以先跳过，等熟悉后再加。
# -----------------------------------------------------------

# mcpServers:
#   - name: filesystem
#     command: npx
#     args: ["-y", "@modelcontextprotocol/server-filesystem", "C:/你的项目路径"]


# -----------------------------------------------------------
# 第七部分：其他杂项
# -----------------------------------------------------------

# 关闭匿名数据收集（保护隐私）
allowAnonymousTelemetry: false
```



## 四、常见问题排查
| 问题           | 解决方案                                                               |
| ------------ | ------------------------------------------------------------------ |
| **连接被拒绝**    | 确认 `ollama serve` 正在运行，且端口为 11434                                  |
| **模型响应极慢**   | 降低 `num_ctx`（如从 64K 降到 32K 或 16K）；或关闭 MCP 工具加速                     |
| **工具调用失败**   | 本地 7B 模型能力有限，比云端大模型更容易出现工具调用错误，可尝试修改 prompt 或使用 Enhance Prompt 按钮  |
| **中文乱码**     | Qwen 系列原生支持中文，如出现问题检查模型是否完整下载，可重新执行 `ollama pull qwen2.5-coder:7b` |
| **显存不足 OOM** | 使用更小的上下文窗口（如 4096 或 8192），或换用更小模型如 `qwen2.5-coder:1.5b`            |





